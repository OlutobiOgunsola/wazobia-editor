import React from 'react'
import Toolbar from './Toolbar';
import TextField from './TextField';

const TextArea = (): React.JSX.Element => {
    return <div>
        <Toolbar />
        <TextField />
    </div>
}

export default TextArea;